{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "model": "0.1.0",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.0",
    "ui": "0.1.0",
    "utils": "0.1.0"
  },
  "scripts": {
    "start": "REACT_APP_DEVICE=web react-app-rewired start",
    "build": "REACT_APP_DEVICE=web react-app-rewired build",
    "test": "react-app-rewired test --env=jsdom"
  },
  "devDependencies": {
    "react-app-rewire-yarn-workspaces": "^1.0.2",
    "react-app-rewired": "^1.4.0"
  }
}
